# Terminus

Terminus is an agent for terminal-based task execution, extracted from Harbor.

## Installation

For local development:

```bash
pip install -e /path/to/terminus
```

## Usage

```python
from terminus import Terminus2

# Initialize and use the agent
agent = Terminus2(...)
```
